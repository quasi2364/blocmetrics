

<table class="table table-hover">
	<thead>
		<tr>
			<th>Event Name</th>
			<th>Count</th>
		</tr>
	</thead>
	<tbody>
		<% @events.each do |key, value| %>
			<tr>
				<td><%= key %></td>
				<td><%= value.count %></td>
			</tr>
		<% end %>
	</tbody>
</table>
